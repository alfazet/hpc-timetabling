# TODO
- Add `config_preferences` to the chromosome.
- Figure out what's wrong with the evaluator (in particular fix the bug that causes solutions to be evaluated as better than they actually are).
- Quality before quantity: smaller populations but devote more memory and blocks for one solution (esp. considering local search).
- Smaller populations while hard constraint reduction, bigger populations for soft constraints